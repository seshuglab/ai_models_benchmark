# Возможные улучшения AI Models Benchmark

Текущая версия программы: `v0.9k`.

## Ближайшая версия

- Выводить статус лишь при его изменении, чтобы не засорять консоль повторяющимися сообщениями.
- Решить, нужны ли разные коды выхода для успеха, ошибок аргументов, ошибок провайдеров и исключений: сейчас программа всегда завершается с кодом 0, что мешает автоматизации; при изменении сохранить ожидание Enter и обновить тесты и документацию.

## Метрики OpenCode

- Решить, как обозначать неполные метрики, если OpenCode не передал один из ожидаемых `step_finish`.
- Добавить расчётную стоимость запуска в отчёт по проверенной формуле `input*pin + (output+reasoning)*pout + cache*pcache` из `opencode models --verbose` (сверка с фактом 2026-09-17 сошлась до цента).

## Оставшиеся юнит-тесты

- Добавить тесты `get_tests` на `tempfile`: маска `[0-9][0-9]_*.md`, пропуск файлов без `#`, корректные `title/тело`.
- Добавить тесты `save_report` на `tempfile`: порядок секций, ветка `error`, агентные метрики только для агентного протокола, загрузка модели только для протокола генерации.

## Диагностика событий OpenCode

- Безопасно обрабатывать сбой потока OpenCode: завершать оставшийся процесс и сохранять уже полученные метрики, события и JSON-журнал.
- Решить удобный формат диагностического `.log`: читаемость текстом по шагам (как думает агент) против структурированного JSON для машинного разбора; пока приоритет — текст по блокам.

## Результаты агентных запусков

- Проверить, достаточно ли отчёта, когда агент создаёт код в `.agent_work`, но не показывает его в финальном ответе.
- Рассмотреть добавление в отчёт краткого списка созданных файлов без анализа их содержимого.

## Отложенные замечания ревью (проверять при проблемах)

- При бессрочном зависании агентного запуска проверить переполнение `stderr`: в `run_opencode_cli_test` создан `stderr=PIPE`, который читается только после исчерпания stdout; ребёнок с объёмным выводом в `stderr` (>64 КБ) вызовет взаимный дедлок. Лечение: `stderr=DEVNULL` либо фоновая откачка в отдельном потоке.
- При разъезжании колонок проверить вычисление ширины через `len()`: баннер, сетка моделей и выравнивание провайдеров не учитывают двойную ширину восточноазиатских и псевдографических символов.
- При пропавшем отчёте проверить коллизию имён: два запуска в одну секунду дают одинаковое имя `ai_test_*` и перезаписывают друг друга.
- При росте тестов объединить три хелпера изолированного прогона (`run_with_test_choice`, `run_isolated`, `run_scenario`) в один параметризованный.
- При расхождении переводов помнить о дубле документации: `README.md` и `README_EN.md` поддерживаются вручную.
